"""HTTP client for the Recipe Service.

Currently not needed — the recipe import flow is purely event-driven
(publish recipe.imported via RabbitMQ). This module is a placeholder
for future status update calls if needed.
"""
